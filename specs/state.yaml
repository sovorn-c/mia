# specs/state.yaml — Active session state
session:
  active_flow: "plan_work"
  active_epic_id: "e02"
  active_story_id: "e02s01"
  target_version: "0.2.0"

handoff:
  next_skill: "plan-work"
  summary: "Epic e02 planned across 4 stories (e02s01..e02s04) with prompt_toolkit integration, carrot_bounce working stream, and post-turn inspection."

git:
  branch: "main"
