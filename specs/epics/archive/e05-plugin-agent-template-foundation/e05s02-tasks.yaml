story_id: e05s02
story_title: "Per-Agent Plugin Lifecycle and Integrity Boundary"
spec: "e05s02-plugin-lifecycle-integrity.md"
status: passing
requirement_delta: MODIFIED
bcps: 8
tasks:
  - id: e05s02t01
    title: "Lock lifecycle and integrity failures with contract tests"
    description: "Specify per-Agent list/show/configure/disable behavior, malformed or incompatible Plugin rejection, no partial activation, Agent isolation, and preserved data."
    status: passing
    risk: P0
    security: high
    depends_on: []
    verify: >-
      uv run --offline pytest tests/test_plugins.py tests/test_notes_plugin.py -k
      'lifecycle or configure or disable or malformed or incompatible or partial or isolation'
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Plugin", "lifecycle", "integrity", "Security Review"]

  - id: e05s02t02
    title: "Validate installed manifests and atomic per-Agent configuration"
    description: "Harden Plugin identity/version/contribution validation and persist explicit Agent enablement/configuration atomically without secrets or silent Tool/access expansion."
    status: passing
    risk: P0
    security: high
    depends_on: [e05s02t01]
    verify: >-
      uv run --offline pytest tests/test_plugins.py tests/test_agents.py -k
      'manifest or version or config or atomic or secret or capability or full_access'
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Plugin", "Agent", "configuration", "Security Review"]

  - id: e05s02t03
    title: "Enforce Agent-scoped Notes isolation and non-destructive disablement"
    description: "Keep Notes data under the owning Agent, prevent cross-Agent reads, and remove capabilities from later Runs when disabled without deleting stored notes."
    status: passing
    risk: P0
    security: high
    depends_on: [e05s02t02]
    verify: >-
      uv run --offline pytest tests/test_notes_plugin.py tests/test_orchestration.py -k
      'agent_scope or isolation or disable or preserve or later_run or traversal'
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Notes", "isolation", "runtime", "Security Review"]

  - id: e05s02t04
    title: "Carry optional Plugin attribution through Tool execution"
    description: "Add backward-compatible Plugin identity to Tool context, call/result events, and redacted audit records while preserving existing renderers and Plugin-free events."
    status: passing
    risk: P0
    security: high
    depends_on: [e05s02t02]
    verify: >-
      uv run --offline pytest tests/test_plugins.py tests/test_agent_loop.py tests/test_access_policy.py
      tests/test_cli_print_mode.py tests/test_cli_repl.py -k
      'plugin_id or attribution or audit or redact or tool_event or plugin_free'
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Plugin", "events", "telemetry", "Security Review"]

  - id: e05s02t05
    title: "Complete Plugin lifecycle commands and diagnostics"
    description: "Expose installed/available inspection, per-Agent configuration and disablement, and clear missing/malformed/incompatible diagnostics through thin CLI adapters."
    status: passing
    risk: P0
    security: medium
    depends_on: [e05s02t02, e05s02t03, e05s02t04]
    verify: >-
      uv run --offline pytest tests/test_plugin_cli.py tests/test_cli_print_mode.py -k
      'list or show or configure or disable or malformed or incompatible or missing'
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Plugin", "CLI", "diagnostics", "Security Review"]

  - id: e05s02t06
    title: "Close lifecycle compatibility and quality gates"
    description: "Run Agent, runtime, Delegation, access, event, Session, CLI, and full project verification with Plugin-free and disabled-Plugin regressions."
    status: passing
    risk: P0
    security: high
    depends_on: [e05s02t03, e05s02t04, e05s02t05]
    verify: >-
      uv run --offline ruff format --check .
      && uv run --offline ruff check .
      && uv run --offline mypy src
      && uv run --offline pytest tests/test_plugins.py tests/test_notes_plugin.py
      tests/test_plugin_cli.py tests/test_agents.py tests/test_orchestration.py
      tests/test_agent_loop.py tests/test_access_policy.py tests/test_delegation.py
      tests/test_sessions.py tests/test_cli_print_mode.py tests/test_cli_repl.py
      && ./scripts/check-coverage.sh
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Plugin", "regression", "quality gate", "Security Review"]
