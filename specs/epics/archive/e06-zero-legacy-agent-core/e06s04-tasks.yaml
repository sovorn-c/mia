story_id: e06s04
title: Purge Project Surfaces and Verify the Clean Break
spec: e06s04-purge-project-surfaces-clean-break.md
status: passing
bcps: 5
risk: P1
delta: MODIFIED
scenarios:
  - SC-e06s04-P1-01
  - SC-e06s04-P1-02
  - SC-e06s04-P1-03
  - SC-e06s04-P1-04
  - SC-e06s04-P1-05
tasks:
  - id: e06s04t01
    title: Rewrite active guidance and specifications
    status: passing
    risk: P1
    depends_on: [e06s03t06]
    story_steps: [1]
    scenarios: [SC-e06s04-P1-01]
    work:
      - Keep contributor guidance byte-identical across both instruction files.
      - Rewrite README, product language, architecture, security, impact, and planning context around Agent.
    files:
      - AGENTS.md
      - CLAUDE.md
      - README.md
      - specs/product/
      - specs/tech-architecture/tech-stack.md
      - specs/security/REVIEW.md
      - specs/IMPACT_LATEST.md
      - specs/planning-context.yaml
    verify: cmp -s AGENTS.md CLAUDE.md && ./scripts/check-public-surface.sh
  - id: e06s04t02
    title: Remove obsolete repository artifacts
    status: passing
    risk: P1
    depends_on: [e06s04t01]
    story_steps: [2]
    scenarios: [SC-e06s04-P1-02]
    work:
      - Remove old planning capsules, snapshots, and obsolete verification evidence.
      - Update release, execution, state, and epic metadata to reference existing artifacts.
    files:
      - specs/epics/
      - specs/product/snapshots/
      - specs/verifications/
      - specs/release-plan.yaml
      - specs/execution-status.yaml
      - specs/state.yaml
    verify: >-
      test -f specs/epics/e06-zero-legacy-agent-core/epic.yaml
      && uv run --offline python -c "import pathlib,yaml; [yaml.safe_load(p.read_text()) for p in pathlib.Path('specs').rglob('*.yaml')]"
  - id: e06s04t03
    title: Protect the retained Textual package
    status: passing
    risk: P1
    depends_on: [e06s04t02]
    story_steps: [3]
    scenarios: [SC-e06s04-P1-03]
    work:
      - Keep Textual declared and retain the TUI command and package files.
      - Inspect the wheel for the retained TUI and clean package boundaries.
    files:
      - pyproject.toml
      - src/mia_cli/tui/
      - tests/test_tui_app.py
    verify: >-
      grep -q 'textual>=' pyproject.toml
      && wheel_dir=$(mktemp -d)
      && trap 'rm -rf "$wheel_dir"' EXIT
      && uv build --offline --out-dir "$wheel_dir"
      && uv run --offline python scripts/check-wheel-surface.py "$wheel_dir"/*.whl
  - id: e06s04t04
    title: Run the clean-break quality gate
    status: passing
    risk: P1
    depends_on: [e06s04t03]
    story_steps: [4]
    scenarios: [SC-e06s04-P1-04, SC-e06s04-P1-05]
    work:
      - Run the public-surface scan, YAML validation, consistency check when available, and full quality gate.
      - Record verification evidence only after one contiguous passing run.
    files:
      - scripts/check-public-surface.sh
      - specs/verifications/e06s04-verify.yaml
      - src/
      - tests/
    verify: >-
      cmp -s AGENTS.md CLAUDE.md
      && ./scripts/check-public-surface.sh
      && uv run --offline pytest
      && ./scripts/check-coverage.sh
      && uv run --offline ruff format --check .
      && uv run --offline ruff check .
      && uv run --offline mypy src
      && uv build --offline
