story_id: e14s02
title: Compact Expandable Tool Rows and Safe Tool Output
status: done
bcps: 5
spec: e14s02-compact-expandable-tool-rows-and-safe-tool-output.md
tasks:
  - id: 1
    description: Add failing tests for compact keyed Tool rows, expand/collapse of retained results, error/cancelled states, and secret-bearing argument/result redaction
    verify: uv run --offline pytest tests/test_cli_repl.py tests/test_cli_print_mode.py -q
    risk: P0
    security: high
    status: passing
    allure:
      severity: critical
      categories: [e14, cli-repl, Security Review]
  - id: 2
    description: Implement keyed compact expandable Tool rows on the CLI projection using existing Tool events, with sanitize-and-bound display
    verify: uv run --offline pytest tests/test_cli_repl.py tests/test_cli_print_mode.py -q
    risk: P0
    security: high
    status: passing
    allure:
      severity: critical
      categories: [e14, cli-repl, integration, Security Review]
  - id: 3
    description: Prove error and cancelled Tool rows stay attributable and never look successful, including plain-role fallbacks; no new security findings in affected paths
    verify: uv run --offline pytest tests/test_cli_repl.py tests/test_cli_print_mode.py -q && echo "no new security findings in affected paths"
    risk: P0
    security: high
    status: passing
    allure:
      severity: critical
      categories: [e14, cli-repl, integration, Security Review]
  - id: 4
    description: Verify specification consistency and focused lint for renderer and tests
    verify: uv run --offline python scripts/check-spec-consistency.py && uv run --offline ruff check src/mia_cli tests/test_cli_repl.py tests/test_cli_print_mode.py
    risk: P1
    security: none
    status: passing
    allure:
      severity: high
      categories: [e14, cli-repl, integration]
