story_id: e13s01
title: Truthful Inline Run and Stream Presentation
status: failing
bcps: 4
spec: e13s01-truthful-inline-run-and-stream-presentation.md
tasks:
  - id: 1
    description: Add focused tests for truthful idle/running/approval/completed/cancelled/error presentation and attributed stream/Tool grouping
    verify: uv run --offline pytest tests/test_cli_repl.py tests/test_cli_print_mode.py -q
    risk: P1
    status: failing
    allure:
      severity: high
      categories: [e13, cli-repl, unit]
  - id: 2
    description: Implement compact inline status and stream presentation through the existing REPL adapter without changing Core event semantics
    verify: uv run --offline pytest tests/test_cli_repl.py tests/test_cli_print_mode.py -q
    risk: P1
    status: failing
    allure:
      severity: high
      categories: [e13, cli-repl, integration]
  - id: 3
    description: Verify specification, lint, and focused CLI contracts after the presentation change
    verify: uv run --offline python scripts/check-spec-consistency.py && uv run --offline ruff check src/mia_cli tests/test_cli_repl.py tests/test_cli_print_mode.py
    risk: P1
    status: failing
    allure:
      severity: high
      categories: [e13, cli-repl, integration]
