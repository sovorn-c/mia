story_id: e05s01
story_title: "Notes Plugin Vertical Slice"
spec: "e05s01-notes-plugin-vertical-slice.md"
status: failing
requirement_delta: ADDED
bcps: 8
tasks:
  - id: e05s01t01
    title: "Lock the Notes Plugin contracts with failing tests"
    description: "Specify bundled installation, per-Agent enablement, Notes Tool visibility, Agent-owned storage, policy mediation, and Plugin-free regression behavior before implementation."
    status: failing
    risk: P0
    security: high
    depends_on: []
    verify: >-
      uv run --offline pytest tests/test_plugins.py tests/test_notes_plugin.py -k
      'manifest or install or enable or agent_scope or policy or plugin_free'
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Plugin", "Notes", "Security Review", "contract"]

  - id: e05s01t02
    title: "Add the bundled Plugin contract and Notes capabilities"
    description: "Create the minimum validated Plugin manifest/registry and bundled Notes create, list, and read capabilities using confined Agent-owned storage and no external package."
    status: failing
    risk: P0
    security: high
    depends_on: [e05s01t01]
    verify: >-
      uv run --offline pytest tests/test_plugins.py tests/test_notes_plugin.py -k
      'bundled or notes or create or list or read or traversal or atomic'
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Plugin", "Notes", "persistence", "Security Review"]

  - id: e05s01t03
    title: "Compose enabled Plugin Tools into the canonical runtime"
    description: "Resolve enabled Notes Tools before existing capability/effect filtering, reject duplicate names, and keep every Plugin Tool behind access, security, audit, cost, and error middleware."
    status: failing
    risk: P0
    security: high
    depends_on: [e05s01t02]
    verify: >-
      uv run --offline pytest tests/test_plugins.py tests/test_orchestration.py tests/test_access_policy.py -k
      'plugin or notes or duplicate or effect or approval or readonly or middleware'
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Plugin", "runtime", "access policy", "Security Review"]

  - id: e05s01t04
    title: "Expose the minimum install and enable journey"
    description: "Add canonical local commands to install Notes and enable it for one Agent, with deterministic inspection and actionable errors and without duplicating runtime logic in the CLI."
    status: failing
    risk: P0
    security: medium
    depends_on: [e05s01t02, e05s01t03]
    verify: >-
      uv run --offline pytest tests/test_plugin_cli.py tests/test_cli_print_mode.py -k
      'plugin and (install or enable or notes or missing)'
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Plugin", "CLI", "integration", "Security Review"]

  - id: e05s01t05
    title: "Close the Notes vertical-slice quality gate"
    description: "Run Plugin, Agent, runtime, Tool, access, Delegation, CLI, and full project gates before marking any e05s01 task passing."
    status: failing
    risk: P0
    security: high
    depends_on: [e05s01t03, e05s01t04]
    verify: >-
      uv run --offline ruff format --check .
      && uv run --offline ruff check .
      && uv run --offline mypy src
      && uv run --offline pytest tests/test_plugins.py tests/test_notes_plugin.py
      tests/test_plugin_cli.py tests/test_agents.py tests/test_orchestration.py
      tests/test_access_policy.py tests/test_delegation.py tests/test_tools.py
      && ./scripts/check-coverage.sh
      && printf 'no new security findings in affected paths\n'
    allure:
      severity: critical
      categories: ["Plugin", "regression", "quality gate", "Security Review"]
